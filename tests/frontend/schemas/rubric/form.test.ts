import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/rubric/form';

describe('rubric/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
