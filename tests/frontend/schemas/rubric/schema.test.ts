import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/rubric/schema';

describe('rubric/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
