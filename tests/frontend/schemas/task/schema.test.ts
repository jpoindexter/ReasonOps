import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/task/schema';

describe('task/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
