import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/completion/schema';

describe('completion/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
