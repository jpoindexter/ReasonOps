import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/completion/form';

describe('completion/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
