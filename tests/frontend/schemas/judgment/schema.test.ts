import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/judgment/schema';

describe('judgment/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
