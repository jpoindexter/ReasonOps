import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/scoring/form';

describe('scoring/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
