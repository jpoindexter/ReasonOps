import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/scoring/schema';

describe('scoring/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
