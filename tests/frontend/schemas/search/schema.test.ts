import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/search/schema';

describe('search/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
