import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/search/form';

describe('search/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
