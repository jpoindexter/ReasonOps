import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/onboarding/form';

describe('onboarding/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
