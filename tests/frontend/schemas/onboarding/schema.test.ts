import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/onboarding/schema';

describe('onboarding/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
