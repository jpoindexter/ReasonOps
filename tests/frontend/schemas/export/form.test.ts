import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/export/form';

describe('export/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
