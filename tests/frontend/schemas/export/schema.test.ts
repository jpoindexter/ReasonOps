import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/export/schema';

describe('export/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
