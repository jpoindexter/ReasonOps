import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/dashboard/form';

describe('dashboard/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
