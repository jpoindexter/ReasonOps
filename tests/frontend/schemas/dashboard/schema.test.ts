import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/dashboard/schema';

describe('dashboard/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
