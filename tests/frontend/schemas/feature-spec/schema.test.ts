import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/feature-spec/schema';

describe('feature-spec/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
