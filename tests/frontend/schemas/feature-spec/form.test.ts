import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/feature-spec/form';

describe('feature-spec/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
