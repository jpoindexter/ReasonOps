import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/session/form';

describe('session/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
