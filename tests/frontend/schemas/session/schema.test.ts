import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/session/schema';

describe('session/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
