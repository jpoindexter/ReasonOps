import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/reviewer/schema';

describe('reviewer/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
