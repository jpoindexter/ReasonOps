import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/reviewer/form';

describe('reviewer/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
