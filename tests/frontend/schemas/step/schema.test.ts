import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/step/schema';

describe('step/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
