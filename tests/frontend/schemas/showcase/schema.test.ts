import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/showcase/schema';

describe('showcase/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
