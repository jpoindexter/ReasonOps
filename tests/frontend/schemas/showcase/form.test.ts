import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/showcase/form';

describe('showcase/form', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
