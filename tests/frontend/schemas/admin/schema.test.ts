import { describe, expect, it } from 'vitest';
import * as mod from '@schemas/admin/schema';

describe('admin/schema', () => {
  it('should be defined', () => {
    expect(mod).toBeDefined();
  });
});
